<service>
  <id>OpenSCMServer</id>
  <name>OpenSCM Server</name>
  <description>OpenSCM security compliance server — https://openscm.io</description>

  <executable>%BASE%\scmserver.exe</executable>

  <!-- Restart on failure -->
  <onfailure action="restart" delay="5 sec"/>
  <onfailure action="restart" delay="10 sec"/>
  <onfailure action="none"/>

  <!-- Logging -->
  <log mode="roll-by-size">
    <sizeThreshold>10240</sizeThreshold>
    <keepFiles>5</keepFiles>
  </log>

  <!-- Start mode -->
  <startmode>Automatic</startmode>

</service>
